# Example environment variables for the CORS proxy
# Copy this file and set your values

# The origin allowed to access the proxy (your GitHub Pages URL)
ALLOWED_ORIGIN=https://sunix.github.io

# Optional: Security token for authentication
# Uncomment and set a strong random token
# PROXY_TOKEN=your-secret-token-here
